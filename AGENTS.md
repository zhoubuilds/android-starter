# 智能体执行规范

## 修订记录

| 修订时间（CST） | 修订人  | 修订说明                         |
|-----------------|---------|----------------------------------|
| 2026-08-31      | whisper | 收敛为智能体专属执行与授权规则   |
| 2026-08-28      | whisper | 明确全部提交不包含排除项         |
| 2026-08-27      | whisper | 接入独立开发规范                 |
| 2026-08-26      | whisper | 建立 Starter 智能体执行规范      |

## 1. 定位与规范入口

* 本文件只规定智能体特有的执行, 授权和指令解释规则, 不作为人工维护者的通用项目规范.
* 开始修改前必须阅读 [代码规范](doc/application/code-style.md), [开发规范](doc/application/development.md) 和受影响模块的
  `design.md`, `development.md`, `usage.md`. 这些文档适用于所有维护者, 智能体同样必须遵守.
* 通用命名, 编码, 架构, 验证, 文档和 Git 规则只在上述项目规范中维护. 本文件通过链接引用, 不复制第二套规则.
* 本文件只在项目规范之上增加智能体限制, 不改变模块边界, 公开契约或项目规范的适用范围.

## 2. 智能体执行边界

* 变更判断, 范围控制和工作区处理必须执行 [开发规范](doc/application/development.md) 第 1 至 3 节.
* 用户描述与代码事实, 构建结果或项目文档不一致时, 明确指出差异和影响.
* 用户对局部修改的授权不能解释为删除数据, 重写 Git 历史, 修改受保护契约或执行其它范围外操作的授权.
* 需要扩大任务范围或执行不可逆操作时, 先说明原因, 影响和风险并取得明确授权.

## 3. Aegis 契约保护

* Aegis 主要用于约束智能体修改. `@aegis` 保护标记覆盖其说明中的公开契约, 可观察行为和实现约束, 不是普通注释.
* 修改前检查源码标记和受影响模块 `development.md` 中的 Aegis 保护范围.
* 修改受保护内容前, 先说明影响范围和风险并取得用户明确授权. 无法证明行为等价时按受保护修改处理.
* 格式, import, 普通注释, private 命名和可证明行为等价的保护范围外重构不需要额外授权.
* 移除 `@aegis`, 缩小保护范围或弱化本节规则同样需要明确授权.
* 获得授权后, 在每个受影响标记下追加审计记录, 不覆盖历史记录:

```kotlin
 * @aegis-audit 2026-08-26 | whisper | 简要说明本次授权修改原因.
```

* 日期使用 CST. 授权人优先使用用户明确身份, 其次使用作者解析规则; 无法确认时询问.
* 契约修改和对应审计记录必须进入同一提交.

## 4. Git 指令解释

* Git 暂存, 拆分, 提交和历史处理遵守 [开发规范](doc/application/development.md).
* 智能体只在用户明确要求时创建提交, 不暂存或提交与当前任务无关的修改.
* 用户要求"提交所有本地文件", "提交全部修改", "提交本地所有内容"或其它同义操作时, "所有"只表示当前工作区中适合
  版本控制的修改, 不表示磁盘上的字面全部文件.
* 上述宽泛指令不授权使用 `git add -f`, 也不包含被 `.gitignore` 或开发规范排除的 IDE 配置, 构建产物, 本机配置,
  临时文档, 日志, 缓存, 签名文件, 服务配置, 凭据和敏感数据.
* 只有用户明确点名某个本应排除的文件, 或明确要求包含本应忽略的文件时, 才能单独评估是否纳入; 宽泛的"所有"永远
  不构成该授权. 涉及密钥, 凭据, 签名材料或隐私数据时, 即使文件被点名也必须先说明风险并再次取得明确确认.
