# 智能体协作规范

## 修订记录

| 修订时间（CST） | 修订人  | 修订说明                         |
|-----------------|---------|----------------------------------|
| 2026-08-27      | whisper | 接入独立开发规范                 |
| 2026-08-26      | whisper | 建立 Starter 智能体执行规范      |

## 1. 规范入口

* 修改前阅读 [代码规范](doc/application/code-style.md), [开发规范](doc/application/development.md) 和受影响模块的 `design.md`,
  `development.md`, `usage.md`.
* 本文件只补充适合智能体执行的约束. 命名, 编码和格式以代码规范为准; 变更决策, 范围拆分, 验证, 文档同步和 Git 提交以开发规范为准;
  模块边界和公开契约以模块文档为准.
* Starter 是公共模板, 不写入具体应用的真实域名, token, 错误码, 业务响应假设, 渠道或页面规则.

## 2. 协作原则

* 以代码事实, 构建结果和项目文档为依据. 用户描述与仓库现状不一致时, 明确指出差异和影响.
* 保留工作区中已有且与当前任务无关的修改, 不擅自恢复, 删除, 取消暂存或纳入提交.
* 修改范围保持聚焦. 非必要的格式化, 依赖升级, 目录调整和公开 API 重构不与当前任务混合.
* 优先使用项目已有的模块边界, 插件和工具 API. 新增抽象必须能减少真实复杂度或匹配现有模式.
* 发现实现与文档冲突时, 先判断当前事实和设计意图, 再同步修复实现或文档, 不静默保留冲突.

## 3. 命名执行

* 包名用于归类, 不能代替类型角色后缀.
* 领域模型使用完整领域语义命名, 不强制后缀.
* 数据库存储模型使用 `Entity`, 网络请求 / 响应使用 `Req` / `Resp`.
* UI 渲染模型使用 `UiModel`, 持续状态使用 `UiState`, 一次性行为使用 `UiEffect`.
* AndroidX ViewModel 使用 `ViewModel`, 异常使用 `Exception`.
* 不新增 `VO`, `VM`, `UiMessageUiModel` 或无明确限定的自定义 `Message` 类型.
* 类型重命名时同步调整文件名, 构造调用, 转换函数, 测试和文档.

## 4. 注释和文档

* 新增普通顶层 Kotlin 类型时添加中文 KDoc.
* `@author` 优先读取根目录 `local.properties` 的 `author`; 缺失时读取 Git 作者配置; 仍无法确认时询问维护者.
* `@since` 使用类型创建日期, 格式为 `yyyy/MM/dd`.
* 修改 Markdown 文档时同步更新其修订记录. 修订人使用上述作者解析规则, 不改写历史记录.
* 注释描述职责, 原因, 契约和限制, 不复述代码.

## 5. Aegis 契约保护

* `@aegis` 保护标记覆盖其说明中的公开契约, 可观察行为和实现约束, 不是普通注释.
* 修改受保护内容前, 先说明影响范围和风险并取得用户明确授权. 无法证明行为等价时按受保护修改处理.
* 格式, import, 普通注释, private 命名和可证明行为等价的保护范围外重构不需要额外授权.
* 移除 `@aegis`, 缩小保护范围或弱化本节规则同样需要明确授权.
* 获得授权后, 在每个受影响标记下追加审计记录, 不覆盖历史记录:

```kotlin
 * @aegis-audit 2026-08-26 | whisper | 简要说明本次授权修改原因.
```

* 日期使用 CST. 授权人优先使用用户明确身份, 其次使用作者解析规则; 无法确认时询问.
* 契约修改和对应审计记录必须进入同一提交.

## 6. 架构和构建边界

* architecture 只表达通用技术抽象, 不解释应用响应, Meta, 错误码, 域名, 鉴权或具体页面语义.
* foundation 实现应用业务基座中的响应映射, 错误解释, Meta 建模和领域转换.
* app 作为组合根选择环境, 域名和应用级实现, 下层模块不读取 app `BuildConfig` 或感知 flavor.
* `build-logic` 可以提供公共约定插件; Prism 必须保持为可选的 BuildConfig 生成能力.
* 修改 Prism, Aster, Habitat, Quill 或跨模块公开契约时, 先阅读对应模块文档并扩大验证范围.

## 7. 可测试性和验证

* 不得只为测试改变生产行为, 放宽公开 API, 增加无业务价值接口或暴露内部状态.
* 优先通过合理职责边界, 协作者注入, 纯转换和集成验证提高可测试性.
* 验证范围随风险扩大: 局部实现至少运行相关测试; 公共模块运行调用方编译; 构建逻辑覆盖受影响插件和变体.
* 修改协程, Flow, 缓存, 并发, 生成代码或变体选择时, 必须验证对应边界行为, 不能只以 Kotlin 编译通过作为完成标准.
* 无法执行所需验证时, 在结果中明确列出未验证项和原因.

## 8. Git 执行

* Git 暂存, 拆分和提交遵守 [开发规范](doc/application/development.md).
* 智能体只在用户明确要求时创建提交, 不暂存或提交与当前任务无关的修改.
